8) Write a program to count the total number of distinct elements in an array of length n.


#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Enter number of elements: ";
    cin >> n;
    int A[n];

    cout << "Enter elements: ";
    for (int i = 0; i < n; i++)
        cin >> A[i];

    int diff = 0;
    for (int i = 0; i < n; i++) {
        int j;
        for (j = 0; j < i; j++) {
            if (A[i] == A[j]) 
                break;  
        }
        if (j == i) 
            diff++;
    }

    cout << "Total distinct elements = " << diff << endl;
    return 0;
}
